def count_in_list(lst, item):
    """
    Count the number of occurrences of item in a list.

    Args:
        lst (list): list of elements
        item: element to count

    Returns:
        int: number of occurrences
    """
    return lst.count(item)