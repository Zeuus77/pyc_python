[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ft_package"
version = "0.0.1"
description = "My package"
authors = [
    {name = "youel-id"}
]
license = {text = "MIT"}
requires-python = ">=3.8"